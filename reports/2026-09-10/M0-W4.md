# Laporan M0-W4 — run-id M0-20260910-001

> Gate M0: G-M0-1 (parsial, shard asli belum ada) · G-M0-2 HIJAU · G-M0-3 HIJAU.

## Hasil

| Item | Bukti | Status |
|---|---|---|
| Unit Mojo | `pixi run test-m0`: 21/21 (11 kasus + 2 properti LCG 20 header + 5 baru) | HIJAU |
| Fuzz G-M0-3 | 21/21 korpus (18 tipe error + 3 kontrol PASS), timeout 10s, 0 hang | HIJAU |
| E2E CLI | happy/subset/hilang + subset-salah-tempat + duplikat | HIJAU |
| Kontrak I/O | strace: semua read shard ∈ header, total 1878 B = Σ(8+N) | HIJAU |
| Cakupan | 37/37 situs raise terpetakan (`tests/coverage_m0.md`) | HIJAU |
| Perf C3 | median 0,00 dtk (N=5 + 2 warm-up, fixture) — jauh di bawah 1 s | HIJAU |
| Cgroup 6G | match 19/19 di bawah `MemoryMax=6G` | HIJAU |
| Rust | `cargo test` 4/4 (sha2 known-answer + tamper + shape-fidelity ×2) | HIJAU |
| Fmt/lint | mojo format, cargo fmt/clippy -D, ruff, hooks hygiene | HIJAU |
| G-M0-1 penuh | shape-fidelity R hijau (proxy); 4659 butuh 8 shard asli | TUNGGU DOWNLOAD |

## Tambahan hardening pasca-laporan (run-id sama, bukti baru)

- O(n²) → O(1): merge + dup-check via `Dict`; `weight_map` bound 100.000 entri.
- Bukti perf skala-nyata: index synthetic 4659 entri → parse+merge+compare ≈ 9 ms (jauh < 1 s).
- Skema JSON: `total_tensors` = `len(weight_map)` selalu + `assessed_tensors` baru (anti-overload).
- `verify.rs`: tanpa `unwrap_or_else`/`exit` tengah (return kode); size ikut diverifikasi; clippy `-D warnings` bersih.
- Test hilang terisolasi (index ada, shard tak ada → FILE_NOT_FOUND murni).
- Suite pasca-refactor: Mojo 21/21, Rust 4/4, fuzz 21/21, e2e hijau, I/O hijau.

## Lingkungan

CPU governor: (lihat `cpupower`; dev machine) · threads=1 (verdict) · seed 42 (fixture).
Anomali tercatat: properti-test pertama ~4,5 dtk sekali saja per proses (noise, migrasi antar-test).
